name: flutter_ble_peripheral_example
description: Demonstrates how to use the flutter_ble_peripheral util.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.1.3'
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_ble_peripheral:
    path: ../
  flutter_opengl_view: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.1

flutter:
  uses-material-design: true